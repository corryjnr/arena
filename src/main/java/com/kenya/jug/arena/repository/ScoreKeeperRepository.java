package com.kenya.jug.arena.repository;

import com.kenya.jug.arena.model.ScoreKeeper;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ScoreKeeperRepository extends JpaRepository<ScoreKeeper, Long> {
}
